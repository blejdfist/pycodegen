#pragma once

namespace MyEnums {
    enum class Color {
        Red = 0,
        Green,
        Blue,
        Yellow,
        Black,
        White
    };

    enum class Directions {
        Up,
        Down,
        Left,
        Right
    };
}
